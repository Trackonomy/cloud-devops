name: Deploy node-dev to cloud
on:
  workflow_dispatch:
    inputs:
      branch_name:
        description: "node-dev branch name"
        required: true
      environment:
        type: environment
    secrets:
      TFBACKEND:
        required: true
      TFSECRETVARS:
        required: true
env:
  TFVARS: ${{ vars.TFVARS }}

jobs:
  deploy-image-gallery:
    uses: Trackonomy/cloud-devops/.github/workflows/deploy-image-gallery.yaml@main
    secrets: inherit
  #build-image:
    #uses: Trackonomy/cloud-devops/.github/workflows/build-image.yaml@main
    #secrets: inherit
  #deploy-infrastructure:
    #uses: Trackonomy/cloud-devops/.github/workflows/deploy-infrastructure.yaml@main